streamlit>=1.37.0
streamlit-lightweight-charts>=0.7.0
lightweight-charts>=2.0
yfinance>=0.2.0
pandas>=2.0.0
numpy>=1.24.0
ib_insync>=0.9.86
s3fs
st-files-connection
optuna>=3.0
transformers>=4.40.0
torch>=2.0.0
tweepy>=4.14.0        # X API cashtag trending (premarket_monitor.py --x-token)
python-dotenv>=1.0.0
alpaca-py>=0.15.0      # Optional: free real-time market data (falls back to yfinance)
pytest>=7.0.0
pytest-mock>=3.10.0
anthropic>=0.40.0      # LLM chat for single-ticker analysis (api/llm_chat.py)
sqlalchemy>=2.0
bcrypt>=4.0.0
python-multipart>=0.0.9
# ── Mobile API server (api/server.py + trading_api_kit) ──────────────────────
# Declared in trading_api_kit/pyproject.toml but that package is imported from
# /app rather than pip-installed, so its runtime deps must be pinned here too.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.0
python-jose[cryptography]>=3.3
PyJWT>=2.8
httpx>=0.27