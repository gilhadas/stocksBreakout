[pytest]
markers =
    integration: sends real Discord notifications and requires DISCORD_WEBHOOK_URL_LIVE20
    tier3: IB paper integration tests — require IB Gateway on port 7497 (excluded from CI, run locally via `make test-integration`)
